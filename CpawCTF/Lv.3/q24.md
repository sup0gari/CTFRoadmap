# Q24.[Web]Baby's SQLi - Stage 2-
- Q22で取得したURLにアクセスする
- SQLiでフラグを取得する
## SQLインジェクションを考慮していない一般的なSQLの構造
```sql
select * from users where username = 'admin'
select * from users where password = 'password'
```
## SQLインジェクションの仕組み
```sql
select * from users where password = '' or 1=1 -- -
-- passwordが空欄または真の時にレコードが返る
```